# ===========================================
# Faturamento & ROI - Variáveis de ambiente
# ===========================================
# Copie para .env e preencha os valores.
# Na Vercel: configure em Project > Settings > Environment Variables.

# --- Obrigatório (produção e local) ---
# PostgreSQL. Na Vercel: use Vercel Postgres ou conexão externa (Neon, Supabase, etc.).
# Formato: postgresql://USUARIO:SENHA@HOST:PORTA/DATABASE?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/faturamento_roi"

# --- Opcional ---
# Senha de acesso ao app. Se definida, a tela de login será exibida.
# Deixe vazio para acesso livre (útil apenas em ambiente controlado).
APP_PASSWORD=""
